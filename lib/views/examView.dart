import 'package:flutter/material.dart';

class examView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Exam View")),
    );
  }
}
