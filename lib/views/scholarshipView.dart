import 'package:flutter/material.dart';

class scholarshipView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Scholarship View")),
    );
  }
}
