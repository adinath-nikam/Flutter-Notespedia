import 'package:flutter/material.dart';

class jobsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Jobs View")),
    );
  }
}
