import 'package:flutter/material.dart';

class projectMarketPlace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Buy and Sell Projects."),),
    );
  }
}
